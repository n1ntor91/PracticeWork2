package com.smirnov.top.practice12.tasks;

public class Task2 {
    public static int max(int[] array) {

        int max = array[0];

        for (int i = 1; i < array.length; i++) {
            // если текущий элемент больше max,
            // то максимумом станет текущий элемент массива
        }
        return max;
    }
}
